describe("inputBuider", function() {
  it("should be able to get an string and build the activities", function() {
    var uno = 1;
    expect(uno).toEqual(1);
  });
 
   it("should be able to play a Song", function() {
    var uno = 1;
    expect(uno).toEqual(1);
  });
});